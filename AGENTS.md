# Agent Behavior
- **Keep responses concise:** Provide short, direct answers without overly verbose explanations.
<!-- - **Ask before major changes:** For any significant refactoring or deleting of files, outline your plan first and ask for my approval. -->
<!-- - **Incremental steps:** Take things one step at a time. Do not try to rewrite entire modules in a single try. -->

# Project Scope
- **Directory restrictions:** ONLY modify files within this project's directory. You can read files from outside this project's directory.
<!-- - **Hands-off files:** Do NOT modify configuration files (e.g., `manifest.xml`, `monkey.jungle`, `package.json`, etc.) without explicit permission. -->

# Workflow & Verification
- **Test your work:** After editing a file, always run the appropriate build or compilation command (e.g., `make build` or `npm run build`) via bash to ensure you haven't introduced syntax errors.
- **Self-correction:** If the build fails, attempt to read the error logs and fix the issue before telling me you are done.

<!-- # Coding Guidelines  -->
<!-- - [Insert specific language preferences here, e.g., "Use strict types", "Prefer pure functions", etc.]   -->
